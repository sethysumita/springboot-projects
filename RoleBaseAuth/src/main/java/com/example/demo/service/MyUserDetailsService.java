package com.example.demo.service;

/*
 * import org.springframework.beans.factory.annotation.Autowired; import
 * org.springframework.security.core.userdetails.UserDetails; import
 * org.springframework.security.core.userdetails.UserDetailsService; import
 * org.springframework.security.core.userdetails.UsernameNotFoundException;
 * import org.springframework.stereotype.Service;
 * 
 * import com.example.demo.entity.User; import com.example.demo.repo.UserRepo;
 * 
 * @Service public class MyUserDetailsService implements UserDetailsService {
 * 
 * @Autowired UserRepo userRepo;
 * 
 * @Override public UserDetails loadUserByUsername(String username) throws
 * UsernameNotFoundException { User user = userRepo.findByUsername(username);
 * MyUserDetails details = null; if(user!=null) { details= new MyUserDetails();
 * details.setUser(user);
 * 
 * } else { throw new UsernameNotFoundException("user not found"+username); }
 * return details; }
 * 
 * 
 * }
 */